1) Create parent directory
2) npm init [-y] = create package.json < = blueprint for our app
3) create index.ts
4) install packages = > we will use eventually (npm i <name of package>)
5) import the package(s)